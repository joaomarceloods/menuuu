<% if @menus.any? %>
  <div>
    <%= render "public/qrcodes/image_link", destination_url: public_business_url(current_user.business) %>
  </div>
<% end %>

<h1>
  <%= current_user.business.name %>
</h1>

<%= button_to "Create new menu", [:private, :menus] %>

<ul id="menus">
  <% @menus.each do |menu| %>
    <li><%= link_to menu.name, [:private, menu] %></li>
  <% end %>
</ul>
