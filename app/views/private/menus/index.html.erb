<%= content_for :navigation_bar do %>
  <%= button_to private_menus_path do %>
    <span class="material-symbols-sharp">add</span>
    New menu
  <% end %>
<% end %>

<div class="main-content">
  <%= turbo_frame_tag @business do %>
    <%# TODO: validate empty %>
    <%= form_for @business, url: [:private, :business], html: { oninput: "this.requestSubmit()" } do |f| %>
      <%= f.text_field :name, placeholder: "Business Name", autocomplete: :off, class: "lite-input large-text" %>
    <% end %>
  <% end %>

  <ul id="menus">
    <% @menus.each do |menu| %>
      <li class="small-text"><%= link_to menu.name, [:private, menu] %></li>
    <% end %>
    <li>
      <%= button_to private_menus_path, class: "button" do %>
        New menu…
      <% end %>
    </li>
  </ul>
</div>
