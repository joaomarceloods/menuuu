<%# The `do; end` block prevents the partial from rendering the whole page inside it when it calls `yield`. %>
<%= render "banner", title: notice.html_safe, icon: "warning" do; end if notice.present? %>
<%= render "banner", title: alert.html_safe, icon: "warning" do; end if alert.present? %>
